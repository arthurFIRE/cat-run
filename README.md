# Cat Run

`three.js` 로 만든 2D 횡스크롤 고양이 런 게임입니다.

## 실행

정적 파일이라 간단한 로컬 서버로 실행하면 됩니다.

```bash
cd /Users/kiman/IdeaProjects/catRun
python3 -m http.server 8000
```

브라우저에서 `http://localhost:8000` 을 열면 됩니다.

## 조작

- `Space`
- `ArrowUp`
- `W`
- 화면 탭 / `Jump` 버튼

고양이는 2단 점프가 가능하고, 두 번째 점프에서는 회전합니다.
